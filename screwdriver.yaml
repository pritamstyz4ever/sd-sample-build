shared:
    image: node:8
jobs:
    main:
        requires: [~pr, ~commit]
        steps:
            - s1: echo "Hello"
            - s2: echo "World"
    deploy:
        requires: main
        image: node:8
        environment:
            DEPLOY_ENV: dev
        steps:
            - init: echo $DEPLOY_ENV
            - deploy: echo "deploy"
        freezeWindows:
            - "15 17 * * *"
        # annotations:
        #     # screwdriver.cd/timeout: 1
        #     screwdriver.cd/buildPeriodically: "*/10 * * * *"
    publish:
        steps:
            - echo-hello: echo hello
        requires: ~commit
        freezeWindows:
            - "* * ? * 1"
