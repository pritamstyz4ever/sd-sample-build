shared:
    image: node:8
    annotations:
        beta.screwdriver.cd/my-cluster-annotation: cluster1-data
    buildClusterName: cluster1
jobs:
    main:
        requires: [~pr, ~commit]
        steps:
            - s1: echo "Hello"
            - s2: echo "World"
        #annotations: 
            #screwdriver.cd/buildPeriodically: H H(4-7) * * * 
    deploy:
        requires: main
        image: node:8
        environment:
            DEPLOY_ENV: dev
        steps:
            - init: echo $DEPLOY_ENV
            - deploy: echo "deploy"

    